# The wisp server that performs the proxy's network egress.
# This is the only remote dependency — the Scramjet engine itself is bundled
# into the app and served same-origin. Default is the public anura.pro server.
VITE_WISP_URL=wss://anura.pro/
